import {
    describe,
    test,
    expect
} from "vitest";

describe(
    "Navigation",
    () => {
        test(
            "basic test passes",
            () => {
                expect(
                    true
                ).toBe(true);
            }
        );
    }
);